/** Dark dashboard card shell — deep navy surface for dashboard/home cards. */
export const DASHBOARD_CARD_DARK_CLASS =
  "border border-white/10 bg-[#161b30] text-slate-50 shadow-sm";

/** Same colors with `dark:` variant for components that rely on `.dark` on `html`. */
export const DASHBOARD_CARD_DARK_TW =
  "dark:border-white/10 dark:bg-[#161b30] dark:text-slate-50 dark:shadow-sm";

/** Slightly darker inset (calendar grid, performance menu, range control on `#1B223D`). */
export const DASHBOARD_CARD_INSET_DARK_CLASS = "border-white/10 bg-[#161b30]";
